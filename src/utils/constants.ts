export const NOTE_HEIGHT = 32;
export const PIXELS_PER_BEAT = 80;
export const TICKS_PER_BEAT = 480;
export const PIXELS_PER_TICK = PIXELS_PER_BEAT / TICKS_PER_BEAT;
export const TOTAL_KEYS = 128;
export const DEFAULT_VELOCITY = 100;
export const DEFAULT_DURATION = TICKS_PER_BEAT; // 1 beat default
